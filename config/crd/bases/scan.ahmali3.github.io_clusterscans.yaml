---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterscans.scan.ahmali3.github.io
spec:
  group: scan.ahmali3.github.io
  names:
    kind: ClusterScan
    listKind: ClusterScanList
    plural: clusterscans
    singular: clusterscan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .status.resultsConfigMap
      name: Results
      type: string
    - jsonPath: .status.scanExitCode
      name: Exit Code
      type: integer
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.lastRunTime
      name: Last Run
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterScan is the Schema for the clusterscans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterScanSpec defines the desired state of ClusterScan
            properties:
              command:
                description: Command allows overriding the entrypoint. If empty and
                  Target is specified, defaults to Trivy image scan.
                items:
                  type: string
                type: array
              image:
                description: Image is the scanner container image to run (e.g., aquasec/trivy:latest,
                  aquasec/kube-bench:latest)
                type: string
              schedule:
                description: Schedule is a Cron formatted string. If omitted, the
                  scan runs once.
                type: string
              suspend:
                default: false
                description: Suspend allows pausing the schedule
                type: boolean
              target:
                description: Target is what to scan (e.g., nginx:1.19, python:3.4-alpine).
                  Used for image scanning tools like Trivy.
                type: string
            required:
            - image
            type: object
          status:
            description: ClusterScanStatus defines the observed state of ClusterScan
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastJobName:
                description: LastJobName records the name of the most recent job created
                type: string
              lastRunTime:
                description: LastRunTime records when the job most recently completed
                format: date-time
                type: string
              phase:
                default: Pending
                description: Phase represents the high-level status of the scan (e.g.,
                  Pending, Running, Done, Scheduled)
                type: string
              resultsConfigMap:
                description: ResultsConfigMap points to the ConfigMap containing full
                  scan results
                type: string
              scanExitCode:
                description: ScanExitCode stores the scanner's exit code (0 = success,
                  non-zero = issues found)
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
